- Ajouter le commentaire suivant : "D'autres articles sont disponibles en
  langue anglaise. Pour y accéder, cliquez <a>ici</a>." / "To access additional
  french articles clic <a>here</a>" dans le tag "desc" des pages "articles_xx"
  et "tutoriels_xx".
- Générer une page html pour chaque items (projets) pour y mettre plus
  d'informations sans alourdir la page "projet_xx.html" (instructions pour Git,
  licence, plusieurs screenshots, plusieurs vidéos, ...). 
  Ajouter un fil d'Ariane (police des liens de couleur bordeaux)
- Découper parser.py en plusieurs fonctions
- Passer parser.py à Python 3 (chaînes -> utf-8)
- Améliorer le schema XML :
    <foo lang="en">...</foo>
    <foo lang="fr">...</foo>

    VS

    <foo>
        <en>...</en>
        <fr>...</fr>
    </foo>
- Un seul arbre (document) XML regroupant toutes les pages (peut faire un
  include avec XML pour découper l'arbre en plusieurs fichiers ?). Racine =
  <site>...</site>. => permet d'ajouter des informations globales pour tout le
  site.
- Ajouter l'emprunte de ma clé GPG dans la page "home_**.html" et l'utiliser
  pour signer les paquets et le dépôt Debian.



- Écrire le contenu des pages au format XML :
    - créer un schema (xml schema) -> un schema pour la page principale, un 
      schema pour la page projet, un schema pour la page articles, etc.
    - créer un fichier xslt pour traduire le schema en xhtml
    But :
    - simplifier la traduction en anglais (plusieurs langues dans un seul
      fichier)
    - mieux séparer le contenu de la forme
    - meilleur gestion des tâches automatisées (création de pages pour les
      vidéos, etc.)
- Améliorer l'efficacité de la compilation : Makefile (ou Waf) pour ne pas
  recompiler ce qui n'a pas besoin de l'être
- Améliorer le graphisme (un peu de couleurs...)

Divers :
- sitmaps : http://en.wikipedia.org/wiki/Sitemaps -> générer automatiquement
  le fichier sitemap via le script "crawler.sh"
  (liste les liens html + les dates de dernières modifications)
- meta : description
- remplacer "gitweb" par "browse source code"

