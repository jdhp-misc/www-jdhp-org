A repository is available for Debian and Ubuntu users.
To use it, add the following line in your /etc/apt/sources.list file:

    deb http://www.jdhp.org/dl/debian ./

Then update the list of available packages from the apt sources:

    # apt-get update

